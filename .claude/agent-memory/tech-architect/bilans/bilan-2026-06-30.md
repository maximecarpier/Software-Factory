---
name: bilan-2026-06-30
description: factory-dashboard v3.0 — CDC technique refonte graphique iPhone-first (§13, 10 micro-tâches R1-R10)
metadata:
  type: bilan
---
**Corrections reçues :** aucune — mise à jour cadrée par un brief détaillé.

**Approches validées :**
- Vérification systématique du code AVANT de marquer les micro-tâches v2.1 (T1-T7) complètes : les 7 étaient implémentées, confirmé en lisant store.js/sync.js/api/backlog.js/formView.js/backlogView.js. Ne jamais marquer "fait" sur la foi de la doc.
- Refonte graphique = présentation pure : couche données/offline (store/sync/api/model) explicitement déclarée hors périmètre. A évité de rouvrir des contrats stables.
- Scission `style.css` en R1 (fondations `:root`) + R5 (composants) pour respecter "1 tâche = 1 fichier" malgré le volume — fondations avant composants (dépendance variables CSS).
- R10 (model.js) cadré comme tâche de vérification/no-op probable : `createItem` posait déjà `statut:'à faire'` et `applyFilters` gérait déjà le fallback statut.

**À améliorer :**
- design.md §8.3 indiquait `src/views/nav.js` alors que nav.js est dans `src/components/`. Toujours vérifier les chemins réels (ls) plutôt que de recopier ceux de la doc designer.
- design.md §8.3 disait theme-color `#ffffff`, mais le brief R2 + design §9 disaient `#2563eb` (contradiction interne au design). J'ai tranché sur le brief/manifest. Signaler ce type de contradiction au designer via inter-agent à l'avenir.
