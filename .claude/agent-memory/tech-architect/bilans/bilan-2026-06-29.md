---
name: bilan-2026-06-29
description: factory-dashboard v2.0 — architecture offline-first sans Service Worker, décision boot flush-puis-fetch
metadata:
  type: bilan
---
**Contexte :** Mode update sur `apps/factory-dashboard`. Conception du support offline complet (v2.0) + mise à jour `docs/architecture.md`. Brief utilisateur très prescriptif (pas de SW, sync.js léger, last-write-wins solo, zéro dépendance).

**Corrections reçues :** Aucune — brief complet, pas de Phase Zéro nécessaire (toutes les contraintes données). J'ai produit directement le livrable, ce qui était attendu.

**Approches validées (à reproduire) :**
- Lire le code RÉEL avant de toucher la doc : architecture.md v1.0 était en retard (prétendait « localStorage uniquement, aucun backend » alors que Redis+API existaient). Toujours vérifier le code source comme vérité.
- Décision structurante détectée en lisant le code, non mentionnée dans le brief : `fetchFromGitHub()` au boot écrase le cache local → écraserait des mutations offline non flushées. D'où la **règle d'or boot flush-puis-fetch**. Lire `api/backlog.js` a aussi révélé que le PUT rejette un tableau vide (400) → cas limite documenté.
- Ne pas bloquer sur des Y/N « CHOIX FORT » quand le brief est déjà prescriptif et qu'on est en mode update doc — ça aurait été de l'usine-à-gaz. J'ai documenté les décisions clairement à la place.

**À améliorer :** RAS pour cette session. Le découpage modules croisés M-SYNC ↔ M-STORE (dépendance circulaire potentielle) mérite une vigilance à l'implémentation — noté dans inter-agent.
