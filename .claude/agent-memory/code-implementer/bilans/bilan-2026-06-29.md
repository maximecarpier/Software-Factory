---
name: bilan-2026-06-29
description: factory-dashboard — M-PWA implémentation vite-plugin-pwa
metadata:
  type: bilan
---
**Corrections reçues :** aucune — première session sur ce projet.

**Patterns validés :**
- M-PWA est 100% config (aucun fichier src/ modifié) : `vite.config.js` + `public/manifest.json` + méta HTML suffisent
- `runtimeCaching: []` dans Workbox est la façon correcte d'exclure `/api/backlog` — aucune règle de cache runtime = aucune route API cachée
- `vite-plugin-pwa` génère automatiquement `dist/sw.js` et `dist/workbox-*.js` au build ; pas besoin de script SW manuel
- `public/manifest.json` statique + `manifest` dans plugin config coexistent sans conflit (plugin génère `manifest.webmanifest`, statique reste à `/manifest.json`)
- `base: './'` dans defineConfig doit être maintenu pour Vercel (chemins relatifs)

**À améliorer :** package.json avait été modifié par test-writer entre ma lecture initiale et mon Edit — toujours re-lire avant d'éditer si d'autres agents sont susceptibles d'avoir modifié le fichier.
