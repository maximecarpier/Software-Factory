name: One Piece — Tests

on:
  push:
    paths:
      - "apps/one-piece-speedrun/**"
  pull_request:
    paths:
      - "apps/one-piece-speedrun/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/one-piece-speedrun

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: apps/one-piece-speedrun/package-lock.json

      - run: npm ci

      - run: npm test
